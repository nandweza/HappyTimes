<!DOCTYPE html>
<html lang="en">

    <%- include('./partials/head.ejs') %>

    <body>

        <%- include('./partials/nav.ejs') %>
        <div class="blogs content">
            <div class="title-text">
                <%= blog[0].title %>
            </div>
            <span>
                <%= blog[0].createdAt.toLocaleDateString() %>
            </span>
            <div>
                <%= blog[0].img %>
            </div>
            <p>
                <%= blog[0].content %>
            </p>
        </div>

        <%- include('./partials/footer.ejs') %>
    </body>
</html>